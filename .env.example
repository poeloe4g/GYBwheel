# Copy to .env and fill in. .env is gitignored and never committed.
# No secrets are currently required: option-chain and fundamentals data come
# from yfinance, which needs no credentials. This file documents the pattern
# for when a keyed data source is added (secrets live here, never config.yaml).
